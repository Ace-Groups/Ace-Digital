# Vite production build — used by Firebase Hosting deploy
# Root package.json `build:web:render` sets these automatically; copy for manual builds.

VITE_API_BASE_URL=https://ace-digital-api.onrender.com
VITE_FIREBASE_CHAT=true
BASE_PATH=/
