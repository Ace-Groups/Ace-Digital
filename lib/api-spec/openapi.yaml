openapi: 3.1.0
info:
  # Do not change the title, if the title changes, the import paths will be broken
  title: Api
  version: 0.1.0
  description: Ace Digital OS API specification
servers:
  - url: /api
    description: Base API path
tags:
  - name: health
    description: Health operations
  - name: auth
    description: Authentication
  - name: dashboard
    description: Dashboard
  - name: projects
    description: Projects
  - name: tasks
    description: Tasks
  - name: teams
    description: Teams
  - name: employees
    description: Employees
  - name: finance
    description: Finance
  - name: clients
    description: Clients
  - name: serviceTickets
    description: Service desk tickets
  - name: approvals
    description: Approvals
  - name: reports
    description: Reports
  - name: channels
    description: Channels & Comms
  - name: activity
    description: Activity log

paths:
  /healthz:
    get:
      operationId: healthCheck
      tags: [health]
      summary: Health check
      responses:
        "200":
          description: Healthy
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HealthStatus"

  # AUTH
  /v1/auth/login:
    post:
      operationId: login
      tags: [auth]
      summary: Login
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/LoginInput"
      responses:
        "200":
          description: Login successful
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AuthResponse"
        "401":
          description: Invalid credentials

  /v1/auth/logout:
    post:
      operationId: logout
      tags: [auth]
      summary: Logout
      responses:
        "200":
          description: Logged out

  /v1/auth/me:
    get:
      operationId: getMe
      tags: [auth]
      summary: Get current user
      responses:
        "200":
          description: Current user
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/User"
        "401":
          description: Unauthorized

  /v1/auth/permissions:
    get:
      operationId: getAuthPermissions
      tags: [auth]
      summary: Get current user role and permissions
      responses:
        "200":
          description: Permissions for UI
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AuthPermissions"
        "401":
          description: Unauthorized

  /v1/auth/change-password:
    post:
      operationId: changePassword
      tags: [auth]
      summary: Change password (required on first login when mustChangePassword is true)
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ChangePasswordInput"
      responses:
        "200":
          description: Password updated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/User"
        "400":
          description: Invalid input
        "401":
          description: Unauthorized

  /v1/me:
    get:
      operationId: getMyProfile
      tags: [employees]
      summary: Get own employee profile (includes payslip fields when allowed)
      responses:
        "200":
          description: Profile
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Employee"
        "401":
          description: Unauthorized
    patch:
      operationId: updateMyProfile
      tags: [employees]
      summary: Update own profile (avatar, etc.)
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/MyProfileUpdate"
      responses:
        "200":
          description: Updated profile
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Employee"
        "400":
          description: Invalid input
        "401":
          description: Unauthorized

  /v1/auth/register:
    post:
      operationId: register
      tags: [auth]
      summary: Register user (super_admin only after first)
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/RegisterInput"
      responses:
        "201":
          description: User created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/User"

  # DASHBOARD
  /v1/dashboard:
    get:
      operationId: getDashboard
      tags: [dashboard]
      summary: Get dashboard stats
      responses:
        "200":
          description: Dashboard data
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DashboardData"

  # PROJECTS
  /v1/projects:
    get:
      operationId: listProjects
      tags: [projects]
      summary: List all projects
      parameters:
        - name: status
          in: query
          schema:
            type: string
        - name: teamId
          in: query
          schema:
            type: integer
      responses:
        "200":
          description: List of projects
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Project"
    post:
      operationId: createProject
      tags: [projects]
      summary: Create project
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ProjectInput"
      responses:
        "201":
          description: Project created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Project"

  /v1/projects/{id}:
    get:
      operationId: getProject
      tags: [projects]
      summary: Get project by ID
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Project
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Project"
    patch:
      operationId: updateProject
      tags: [projects]
      summary: Update project
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ProjectUpdate"
      responses:
        "200":
          description: Updated project
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Project"
    delete:
      operationId: deleteProject
      tags: [projects]
      summary: Delete project
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "204":
          description: Deleted

  /v1/projects/{id}/status:
    patch:
      operationId: updateProjectStatus
      tags: [projects]
      summary: Update project status (Kanban)
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ProjectStatusUpdate"
      responses:
        "200":
          description: Updated project
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Project"

  # TASKS
  /v1/tasks:
    get:
      operationId: listTasks
      tags: [tasks]
      summary: List tasks
      parameters:
        - name: projectId
          in: query
          schema:
            type: integer
        - name: teamId
          in: query
          schema:
            type: integer
        - name: assigneeId
          in: query
          schema:
            type: integer
        - name: status
          in: query
          schema:
            type: string
      responses:
        "200":
          description: List of tasks
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Task"
    post:
      operationId: createTask
      tags: [tasks]
      summary: Create task
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/TaskInput"
      responses:
        "201":
          description: Task created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Task"

  /v1/tasks/{id}:
    get:
      operationId: getTask
      tags: [tasks]
      summary: Get task
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Task
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Task"
    patch:
      operationId: updateTask
      tags: [tasks]
      summary: Update task
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/TaskUpdate"
      responses:
        "200":
          description: Updated task
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Task"
    delete:
      operationId: deleteTask
      tags: [tasks]
      summary: Delete task
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "204":
          description: Deleted

  /v1/tasks/{id}/toggle:
    patch:
      operationId: toggleTask
      tags: [tasks]
      summary: Toggle task completion
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Updated task
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Task"

  # TEAMS
  /v1/calendar/feed:
    get:
      operationId: getCalendarFeed
      tags: [calendar]
      summary: Unified calendar feed for a date range
      parameters:
        - name: from
          in: query
          required: true
          schema:
            type: string
            format: date-time
        - name: to
          in: query
          required: true
          schema:
            type: string
            format: date-time
        - name: userId
          in: query
          schema:
            type: integer
        - name: includeTasks
          in: query
          schema:
            type: boolean
            default: true
        - name: includeLinkedChat
          in: query
          schema:
            type: boolean
            default: true
      responses:
        "200":
          description: Calendar feed items
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/CalendarFeedItem"

  /v1/calendar/events:
    get:
      operationId: listCalendarEvents
      tags: [calendar]
      summary: List native calendar events
      parameters:
        - name: from
          in: query
          schema:
            type: string
            format: date-time
        - name: to
          in: query
          schema:
            type: string
            format: date-time
        - name: userId
          in: query
          schema:
            type: integer
      responses:
        "200":
          description: Calendar events
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/CalendarEvent"
    post:
      operationId: createCalendarEvent
      tags: [calendar]
      summary: Create a calendar event
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CalendarEventInput"
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CalendarEvent"

  /v1/calendar/events/{id}:
    patch:
      operationId: updateCalendarEvent
      tags: [calendar]
      summary: Update a calendar event
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CalendarEventUpdate"
      responses:
        "200":
          description: Updated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CalendarEvent"
    delete:
      operationId: deleteCalendarEvent
      tags: [calendar]
      summary: Delete a calendar event
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "204":
          description: Deleted

  /v1/calendar/events/{id}/rsvp:
    patch:
      operationId: rsvpCalendarEvent
      tags: [calendar]
      summary: Update RSVP status on a calendar event
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CalendarRsvpInput"
      responses:
        "200":
          description: Updated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CalendarEvent"

  /v1/calendar/link-chat:
    post:
      operationId: linkChatToCalendar
      tags: [calendar]
      summary: Link a chat event or poll to a user calendar
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CalendarLinkChatInput"
      responses:
        "201":
          description: Linked calendar event
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CalendarEvent"

  /v1/calendar/check-link:
    get:
      operationId: checkCalendarChatLink
      tags: [calendar]
      summary: Check if chat message is linked to user calendar
      parameters:
        - name: channelId
          in: query
          required: true
          schema:
            type: integer
        - name: messageId
          in: query
          required: true
          schema:
            type: integer
        - name: ownerId
          in: query
          schema:
            type: integer
      responses:
        "200":
          description: Link status
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CalendarLinkStatus"

  /v1/teams:
    get:
      operationId: listTeams
      tags: [teams]
      summary: List all teams
      responses:
        "200":
          description: List of teams
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Team"
    post:
      operationId: createTeam
      tags: [teams]
      summary: Create a team
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/TeamInput"
      responses:
        "201":
          description: Team created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Team"

  /v1/job-titles:
    get:
      operationId: listJobTitles
      tags: [employees]
      summary: List job title presets
      responses:
        "200":
          description: Job titles
          content:
            application/json:
              schema:
                type: array
                items:
                  type: string
    post:
      operationId: createJobTitle
      tags: [employees]
      summary: Add a job title preset
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/JobTitleInput"
      responses:
        "201":
          description: Job title saved
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/JobTitleInput"

  /v1/teams/{id}/members:
    get:
      operationId: getTeamMembers
      tags: [teams]
      summary: Get team members
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Team members
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Employee"

  # EMPLOYEES
  /v1/employees:
    get:
      operationId: listEmployees
      tags: [employees]
      summary: List employees
      parameters:
        - name: teamId
          in: query
          schema:
            type: integer
        - name: status
          in: query
          schema:
            type: string
      responses:
        "200":
          description: List of employees
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Employee"
    post:
      operationId: createEmployee
      tags: [employees]
      summary: Create employee
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/EmployeeInput"
      responses:
        "201":
          description: Employee created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EmployeeCreateResponse"

  /v1/employees/next-employee-code:
    get:
      operationId: getNextEmployeeCode
      tags: [employees]
      summary: Preview next auto-generated employee ID
      parameters:
        - name: startDate
          in: query
          schema:
            type: string
            format: date
      responses:
        "200":
          description: Next employee code preview
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/NextEmployeeCode"

  /v1/employees/{id}:
    get:
      operationId: getEmployee
      tags: [employees]
      summary: Get employee
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Employee
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Employee"
    patch:
      operationId: updateEmployee
      tags: [employees]
      summary: Update employee
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/EmployeeUpdate"
      responses:
        "200":
          description: Updated employee
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Employee"
    delete:
      operationId: deleteEmployee
      tags: [employees]
      summary: Delete employee
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "204":
          description: Deleted

  /v1/employees/{id}/reset-password:
    post:
      operationId: resetEmployeePassword
      tags: [employees]
      summary: Reset employee password and require change on next login
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ResetPasswordInput"
      responses:
        "200":
          description: Password reset
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EmployeeCreateResponse"

  # FINANCE
  /v1/finance/summary:
    get:
      operationId: getFinanceSummary
      tags: [finance]
      summary: Get finance summary
      responses:
        "200":
          description: Finance summary
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/FinanceSummary"
        "403":
          description: Forbidden

  /v1/finance/me:
    get:
      operationId: getMyPayslip
      tags: [finance]
      summary: Get own payslip
      responses:
        "200":
          description: Payslip
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PayslipMe"
        "403":
          description: Forbidden

  /v1/finance/salaries:
    get:
      operationId: listSalaries
      tags: [finance]
      summary: List employee salaries
      responses:
        "200":
          description: Salary list
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/SalaryRecord"

  /v1/finance/expenses:
    get:
      operationId: listExpenses
      tags: [finance]
      summary: List expenses
      responses:
        "200":
          description: Expense list
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Expense"
    post:
      operationId: createExpense
      tags: [finance]
      summary: Create expense
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ExpenseInput"
      responses:
        "201":
          description: Expense created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Expense"

  /v1/finance/expenses/{id}:
    patch:
      operationId: patchExpenseStatus
      tags: [finance]
      summary: Approve or reject expense (finance)
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [status]
              properties:
                status:
                  type: string
                  enum: [APPROVED, REJECTED]
      responses:
        "200":
          description: Updated expense
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Expense"
        "403":
          description: Forbidden
        "404":
          description: Not found

  /v1/finance/salary-postings:
    get:
      operationId: listSalaryPostings
      tags: [finance]
      summary: List salary postings (monthly and project allocations)
      responses:
        "200":
          description: Salary postings
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/SalaryPostingRecord"
    post:
      operationId: createSalaryPosting
      tags: [finance]
      summary: Post salary (monthly profile update or project allocation)
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SalaryPostingInput"
      responses:
        "201":
          description: Salary posting created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SalaryPostingRecord"

  /v1/finance/payroll-runs:
    get:
      operationId: listPayrollRuns
      tags: [finance]
      summary: List payroll runs
      responses:
        "200":
          description: Payroll runs
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/PayrollRun"
    post:
      operationId: createPayrollRun
      tags: [finance]
      summary: Create payroll run
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PayrollRunInput"
      responses:
        "201":
          description: Payroll run created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PayrollRun"

  # CLIENTS
  /v1/clients:
    get:
      operationId: listClients
      tags: [clients]
      summary: List clients
      responses:
        "200":
          description: Client list
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Client"
    post:
      operationId: createClient
      tags: [clients]
      summary: Create client
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ClientInput"
      responses:
        "201":
          description: Client created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Client"

  /v1/clients/{id}:
    get:
      operationId: getClient
      tags: [clients]
      summary: Get client
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Client
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Client"
    patch:
      operationId: updateClient
      tags: [clients]
      summary: Update client
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ClientUpdate"
      responses:
        "200":
          description: Updated client
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Client"
    delete:
      operationId: deleteClient
      tags: [clients]
      summary: Delete client
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "204":
          description: Deleted

  # SERVICE TICKETS
  /v1/service-tickets:
    get:
      operationId: listServiceTickets
      tags: [serviceTickets]
      summary: List service tickets
      parameters:
        - name: status
          in: query
          schema:
            type: string
        - name: clientId
          in: query
          schema:
            type: integer
        - name: assigneeId
          in: query
          schema:
            type: integer
        - name: overdueFollowUp
          in: query
          schema:
            type: boolean
      responses:
        "200":
          description: Service ticket list
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/ServiceTicket"
    post:
      operationId: createServiceTicket
      tags: [serviceTickets]
      summary: Create service ticket
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ServiceTicketInput"
      responses:
        "201":
          description: Service ticket created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ServiceTicket"

  /v1/service-tickets/assignees:
    get:
      operationId: listServiceTicketAssignees
      tags: [serviceTickets]
      summary: List employees who can be assigned to service tickets
      responses:
        "200":
          description: Assignee options
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/ServiceTicketAssignee"

  /v1/service-tickets/{id}:
    get:
      operationId: getServiceTicket
      tags: [serviceTickets]
      summary: Get service ticket with recent records
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Service ticket detail
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ServiceTicketDetail"
        "404":
          description: Not found
    patch:
      operationId: updateServiceTicket
      tags: [serviceTickets]
      summary: Update service ticket
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ServiceTicketUpdate"
      responses:
        "200":
          description: Updated service ticket
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ServiceTicket"
        "404":
          description: Not found

  /v1/service-tickets/{id}/records:
    get:
      operationId: listServiceRecords
      tags: [serviceTickets]
      summary: List follow-up records for a ticket
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Service record timeline
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/ServiceRecord"
    post:
      operationId: createServiceRecord
      tags: [serviceTickets]
      summary: Add follow-up record to a ticket
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ServiceRecordInput"
      responses:
        "201":
          description: Record created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ServiceRecordCreateResponse"

  # APPROVALS
  /v1/approvals:
    get:
      operationId: listApprovals
      tags: [approvals]
      summary: List approvals
      parameters:
        - name: status
          in: query
          schema:
            type: string
      responses:
        "200":
          description: Approval list
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Approval"
    post:
      operationId: createApproval
      tags: [approvals]
      summary: Create approval request
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ApprovalInput"
      responses:
        "201":
          description: Approval created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Approval"

  /v1/approvals/{id}/approve:
    post:
      operationId: approveApproval
      tags: [approvals]
      summary: Approve an approval
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: false
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ApprovalReviewInput"
      responses:
        "200":
          description: Approved
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Approval"

  /v1/approvals/{id}/reject:
    post:
      operationId: rejectApproval
      tags: [approvals]
      summary: Reject an approval
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: false
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ApprovalReviewInput"
      responses:
        "200":
          description: Rejected
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Approval"

  # REPORTS
  /v1/reports:
    get:
      operationId: listReports
      tags: [reports]
      summary: List reports
      responses:
        "200":
          description: Report list
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Report"
    post:
      operationId: generateReport
      tags: [reports]
      summary: Generate a report
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ReportInput"
      responses:
        "201":
          description: Report generated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Report"

  # CHANNELS & COMMS
  /v1/channels:
    get:
      operationId: listChannels
      tags: [channels]
      summary: List accessible channels
      responses:
        "200":
          description: Channel list
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Channel"
    post:
      operationId: createChannel
      tags: [channels]
      summary: Create a channel
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ChannelInput"
      responses:
        "201":
          description: Channel created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Channel"
        "400":
          description: Invalid input
        "403":
          description: Forbidden

  /v1/channels/{id}:
    get:
      operationId: getChannel
      tags: [channels]
      summary: Get channel details
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Channel
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Channel"
        "403":
          description: Forbidden
        "404":
          description: Not found
    patch:
      operationId: updateChannel
      tags: [channels]
      summary: Update channel (owners / admins)
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ChannelUpdate"
      responses:
        "200":
          description: Updated channel
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Channel"
        "403":
          description: Forbidden
        "404":
          description: Not found
    delete:
      operationId: deleteChannel
      tags: [channels]
      summary: Delete channel (super admin)
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "204":
          description: Deleted
        "404":
          description: Not found

  /v1/channels/{id}/members:
    get:
      operationId: listChannelMembers
      tags: [channels]
      summary: List channel members
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Members
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/ChannelMember"
    post:
      operationId: addChannelMember
      tags: [channels]
      summary: Add member to channel
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ChannelMemberInput"
      responses:
        "201":
          description: Member added
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ChannelMember"
        "400":
          description: Invalid input
        "403":
          description: Forbidden

  /v1/channels/{id}/members/{userId}:
    delete:
      operationId: removeChannelMember
      tags: [channels]
      summary: Remove member from channel
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
        - name: userId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "204":
          description: Removed
        "400":
          description: Cannot remove last owner
        "403":
          description: Forbidden
        "404":
          description: Not found

  /v1/channels/{id}/mention-candidates:
    get:
      operationId: getChannelMentionCandidates
      tags: [channels]
      summary: List users mentionable in a channel
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
        - name: q
          in: query
          schema:
            type: string
      responses:
        "200":
          description: Mention candidates
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/MentionCandidate"

  /v1/channels/{id}/read:
    post:
      operationId: markChannelRead
      tags: [channels]
      summary: Mark channel as read for current user
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "204":
          description: Marked read
        "403":
          description: Forbidden
        "404":
          description: Not found

  /v1/channels/{id}/messages:
    get:
      operationId: getChannelMessages
      tags: [channels]
      summary: Get channel messages
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
        - name: limit
          in: query
          schema:
            type: integer
            minimum: 1
            maximum: 100
            default: 50
        - name: before
          in: query
          description: Message id cursor for older history
          schema:
            type: integer
      responses:
        "200":
          description: Messages
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Message"
    post:
      operationId: sendMessage
      tags: [channels]
      summary: Send a message
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/MessageInput"
      responses:
        "201":
          description: Message sent
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Message"

  /v1/channels/{id}/messages/{messageId}:
    delete:
      operationId: deleteMessage
      tags: [channels]
      summary: Delete a message (soft delete)
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
        - name: messageId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Message deleted
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Message"
        "403":
          description: Forbidden
        "404":
          description: Not found

  /v1/channels/{id}/messages/{messageId}/reactions:
    post:
      operationId: toggleMessageReaction
      tags: [channels]
      summary: Toggle emoji reaction on a message
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
        - name: messageId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/MessageReactionInput"
      responses:
        "200":
          description: Updated message
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Message"

  /v1/channels/{id}/messages/{messageId}/poll/vote:
    patch:
      operationId: votePoll
      tags: [channels]
      summary: Vote on a poll message
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
        - name: messageId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PollVoteInput"
      responses:
        "200":
          description: Updated poll message
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Message"

  /v1/channels/{id}/messages/{messageId}/event/rsvp:
    patch:
      operationId: rsvpEvent
      tags: [channels]
      summary: RSVP to an event message
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
        - name: messageId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/EventRsvpInput"
      responses:
        "200":
          description: Updated event message
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Message"

  # ACTIVITY
  /v1/activity:
    get:
      operationId: listActivity
      tags: [activity]
      summary: List recent activity
      parameters:
        - name: limit
          in: query
          schema:
            type: integer
      responses:
        "200":
          description: Activity log
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/ActivityLog"

  # NOTIFICATIONS
  /v1/notifications:
    get:
      operationId: listNotifications
      tags: [activity]
      summary: List notifications for current user
      responses:
        "200":
          description: Notifications
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Notification"

  /v1/notifications/read-all:
    post:
      operationId: markAllNotificationsRead
      tags: [activity]
      summary: Mark all notifications as read for current user
      responses:
        "204":
          description: All marked read

  /v1/notifications/{id}/read:
    patch:
      operationId: markNotificationRead
      tags: [activity]
      summary: Mark notification as read
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Notification updated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Notification"

components:
  schemas:
    HealthStatus:
      type: object
      required: [status]
      properties:
        status:
          type: string

    # AUTH SCHEMAS
    LoginInput:
      type: object
      required: [email, password]
      properties:
        email:
          type: string
          format: email
        password:
          type: string

    RegisterInput:
      type: object
      required: [email, password, fullName, role, teamId]
      properties:
        email:
          type: string
          format: email
        password:
          type: string
        fullName:
          type: string
        role:
          type: string
        teamId:
          type: integer
        jobTitle:
          type: ["string", "null"]

    AuthResponse:
      type: object
      required: [token, user]
      properties:
        token:
          type: string
        user:
          $ref: "#/components/schemas/User"

    User:
      type: object
      required: [id, email, fullName, role]
      properties:
        id:
          type: integer
        email:
          type: string
        fullName:
          type: string
        role:
          type: string
        teamId:
          type: ["integer", "null"]
        teamName:
          type: ["string", "null"]
        jobTitle:
          type: ["string", "null"]
        avatarUrl:
          type: ["string", "null"]
        status:
          type: string
        mustChangePassword:
          type: boolean
        phone:
          type: ["string", "null"]
        employeeCode:
          type: ["string", "null"]
        startDate:
          type: ["string", "null"]
        createdAt:
          type: string

    ChangePasswordInput:
      type: object
      required: [newPassword]
      properties:
        currentPassword:
          type: string
        newPassword:
          type: string
          minLength: 8

    # DASHBOARD
    AuthPermissions:
      type: object
      required: [role, permissions]
      properties:
        role:
          type: string
        permissions:
          type: array
          items:
            type: string

    PayslipMe:
      type: object
      required: [fullName, baseSalary, bonus, totalPay, payrollStatus]
      properties:
        fullName:
          type: string
        baseSalary:
          type: number
        bonus:
          type: number
        totalPay:
          type: number
        payrollStatus:
          type: string
        jobTitle:
          type: ["string", "null"]
        teamName:
          type: ["string", "null"]

    DashboardData:
      type: object
      required: [activeProjectsCount, employeeCount, monthlyRevenue, pendingApprovalsCount, recentActivity, upcomingDeadlines, teamLoad, widgets]
      properties:
        activeProjectsCount:
          type: integer
        employeeCount:
          type: integer
        monthlyRevenue:
          type: number
        pendingApprovalsCount:
          type: integer
        activeClientsCount:
          type: integer
        contractValueTotal:
          type: number
        myOpenTasksCount:
          type: integer
        overdueServiceFollowUpsCount:
          type: integer
        widgets:
          type: array
          items:
            type: string
        recentActivity:
          type: array
          items:
            $ref: "#/components/schemas/ActivityLog"
        upcomingDeadlines:
          type: array
          items:
            $ref: "#/components/schemas/Project"
        teamLoad:
          type: array
          items:
            $ref: "#/components/schemas/TeamLoad"
        upcomingCalendar:
          type: array
          items:
            $ref: "#/components/schemas/CalendarFeedItem"

    TeamLoad:
      type: object
      required: [teamId, teamName, activeProjects, members]
      properties:
        teamId:
          type: integer
        teamName:
          type: string
        activeProjects:
          type: integer
        members:
          type: integer

    # PROJECTS
    Project:
      type: object
      required: [id, name, status, priority, progress]
      properties:
        id:
          type: integer
        name:
          type: string
        description:
          type: ["string", "null"]
        teamId:
          type: ["integer", "null"]
        teamName:
          type: ["string", "null"]
        priority:
          type: string
        status:
          type: string
        progress:
          type: integer
        deadline:
          type: ["string", "null"]
        budget:
          type: ["number", "null"]
        clientId:
          type: ["integer", "null"]
        clientName:
          type: ["string", "null"]
        createdById:
          type: ["integer", "null"]
        createdAt:
          type: string

    ProjectInput:
      type: object
      required: [name, status, priority]
      properties:
        name:
          type: string
          minLength: 1
        description:
          type: string
        teamId:
          type: integer
        priority:
          type: string
        status:
          type: string
        deadline:
          type: string
        budget:
          type: number
        clientId:
          type: integer

    ProjectUpdate:
      type: object
      properties:
        name:
          type: string
        description:
          type: string
        teamId:
          type: integer
        priority:
          type: string
        status:
          type: string
        progress:
          type: integer
        deadline:
          type: string
        budget:
          type: number
        clientId:
          type: integer

    ProjectStatusUpdate:
      type: object
      required: [status]
      properties:
        status:
          type: string

    # TASKS
    TaskAssignee:
      type: object
      required: [userId, fullName, completed]
      properties:
        userId:
          type: integer
        fullName:
          type: string
        completed:
          type: boolean

    ServiceTicketAssignee:
      type: object
      required: [id, fullName]
      properties:
        id:
          type: integer
        fullName:
          type: string
        teamId:
          type: ["integer", "null"]
        teamName:
          type: ["string", "null"]

    ServiceTicket:
      type: object
      required: [id, ticketNumber, title, linkType, status, priority, category]
      properties:
        id:
          type: integer
        ticketNumber:
          type: string
        title:
          type: string
        description:
          type: ["string", "null"]
        linkType:
          type: string
          enum: [CLIENT, TODO]
        clientId:
          type: ["integer", "null"]
        clientName:
          type: ["string", "null"]
        projectId:
          type: ["integer", "null"]
        projectName:
          type: ["string", "null"]
        taskId:
          type: ["integer", "null"]
        taskTitle:
          type: ["string", "null"]
        assigneeId:
          type: ["integer", "null"]
        assigneeName:
          type: ["string", "null"]
        teamId:
          type: ["integer", "null"]
        priority:
          type: string
        status:
          type: string
        category:
          type: string
        nextFollowUpAt:
          type: ["string", "null"]
        resolvedAt:
          type: ["string", "null"]
        createdById:
          type: integer
        createdByName:
          type: ["string", "null"]
        recordCount:
          type: integer
        lastRecordAt:
          type: ["string", "null"]
        followUpOverdue:
          type: boolean
        createdAt:
          type: string
        updatedAt:
          type: string

    ServiceTicketDetail:
      allOf:
        - $ref: "#/components/schemas/ServiceTicket"
        - type: object
          properties:
            records:
              type: array
              items:
                $ref: "#/components/schemas/ServiceRecord"

    ServiceTicketInput:
      type: object
      required: [title, linkType]
      properties:
        title:
          type: string
          minLength: 1
        description:
          type: string
        linkType:
          type: string
          enum: [CLIENT, TODO]
        clientId:
          type: integer
        projectId:
          type: integer
        taskId:
          type: integer
        assigneeId:
          type: integer
        teamId:
          type: integer
        priority:
          type: string
        status:
          type: string
        category:
          type: string
        nextFollowUpAt:
          type: string

    ServiceTicketUpdate:
      type: object
      properties:
        title:
          type: string
        description:
          type: string
        linkType:
          type: string
          enum: [CLIENT, TODO]
        clientId:
          type: ["integer", "null"]
        projectId:
          type: ["integer", "null"]
        taskId:
          type: ["integer", "null"]
        assigneeId:
          type: ["integer", "null"]
        teamId:
          type: ["integer", "null"]
        priority:
          type: string
        status:
          type: string
        category:
          type: string
        nextFollowUpAt:
          type: ["string", "null"]

    ServiceRecord:
      type: object
      required: [id, ticketId, recordType, body, createdById, createdAt]
      properties:
        id:
          type: integer
        ticketId:
          type: integer
        recordType:
          type: string
        body:
          type: string
        statusAfter:
          type: ["string", "null"]
        nextFollowUpAt:
          type: ["string", "null"]
        createdById:
          type: integer
        authorName:
          type: ["string", "null"]
        createdAt:
          type: string

    ServiceRecordInput:
      type: object
      required: [body]
      properties:
        recordType:
          type: string
        body:
          type: string
          minLength: 1
        statusAfter:
          type: string
        nextFollowUpAt:
          type: string

    ServiceRecordCreateResponse:
      type: object
      required: [record, ticket]
      properties:
        record:
          $ref: "#/components/schemas/ServiceRecord"
        ticket:
          $ref: "#/components/schemas/ServiceTicket"

    Task:
      type: object
      required: [id, title, status, priority, progress]
      properties:
        id:
          type: integer
        title:
          type: string
        projectId:
          type: ["integer", "null"]
        projectName:
          type: ["string", "null"]
        assigneeId:
          type: ["integer", "null"]
        assigneeName:
          type: ["string", "null"]
        assigneeIds:
          type: array
          items:
            type: integer
        assignees:
          type: array
          items:
            $ref: "#/components/schemas/TaskAssignee"
        teamId:
          type: ["integer", "null"]
        teamName:
          type: ["string", "null"]
        priority:
          type: string
        dueDate:
          type: ["string", "null"]
        status:
          type: string
        progress:
          type: integer
        createdById:
          type: ["integer", "null"]
        createdByName:
          type: ["string", "null"]
        createdAt:
          type: string

    TaskInput:
      type: object
      required: [title]
      properties:
        title:
          type: string
          minLength: 1
        projectId:
          type: integer
        assigneeId:
          type: integer
        assigneeIds:
          type: array
          items:
            type: integer
        teamId:
          type: integer
        priority:
          type: string
        dueDate:
          type: string
        status:
          type: string

    TaskUpdate:
      type: object
      properties:
        title:
          type: string
        projectId:
          type: integer
        assigneeId:
          type: integer
        assigneeIds:
          type: array
          items:
            type: integer
        teamId:
          type: integer
        priority:
          type: string
        dueDate:
          type: string
        status:
          type: string

    CalendarEvent:
      type: object
      required: [id, title, eventType, startAt, ownerId, createdById, visibility, createdAt, updatedAt]
      properties:
        id:
          type: integer
        title:
          type: string
        description:
          type: ["string", "null"]
        eventType:
          type: string
          enum: [meeting, deadline, shift, training, company, leave_block, other]
        startAt:
          type: string
        endAt:
          type: ["string", "null"]
        allDay:
          type: boolean
        location:
          type: ["string", "null"]
        color:
          type: ["string", "null"]
        ownerId:
          type: integer
        ownerName:
          type: ["string", "null"]
        createdById:
          type: integer
        createdByName:
          type: ["string", "null"]
        teamId:
          type: ["integer", "null"]
        visibility:
          type: string
          enum: [private, team, org]
        attendeeIds:
          type: array
          items:
            type: integer
        attendeeStatuses:
          type: object
        sourceRef:
          type: object
        createdAt:
          type: string
        updatedAt:
          type: string

    CalendarEventInput:
      type: object
      required: [title, startAt]
      properties:
        title:
          type: string
          minLength: 1
        description:
          type: string
        eventType:
          type: string
          enum: [meeting, deadline, shift, training, company, leave_block, other]
        startAt:
          type: string
        endAt:
          type: string
        allDay:
          type: boolean
        location:
          type: string
        color:
          type: string
        ownerId:
          type: integer
        teamId:
          type: integer
        visibility:
          type: string
          enum: [private, team, org]
        attendeeIds:
          type: array
          items:
            type: integer

    CalendarEventUpdate:
      type: object
      properties:
        title:
          type: string
        description:
          type: string
        eventType:
          type: string
        startAt:
          type: string
        endAt:
          type: string
        allDay:
          type: boolean
        location:
          type: string
        color:
          type: string
        teamId:
          type: integer
        visibility:
          type: string
        attendeeIds:
          type: array
          items:
            type: integer

    CalendarRsvpInput:
      type: object
      required: [status]
      properties:
        status:
          type: string
          enum: [invited, accepted, declined, tentative]

    CalendarLinkChatInput:
      type: object
      required: [channelId, messageId]
      properties:
        channelId:
          type: integer
        messageId:
          type: integer
        ownerId:
          type: integer
        reminderAt:
          type: string

    CalendarLinkStatus:
      type: object
      required: [linked]
      properties:
        linked:
          type: boolean
        eventId:
          type: integer

    CalendarFeedItem:
      type: object
      required: [id, kind, title, startAt, readOnly]
      properties:
        id:
          type: string
        kind:
          type: string
          enum: [event, task, chat_event, chat_poll]
        title:
          type: string
        description:
          type: ["string", "null"]
        eventType:
          type: string
        startAt:
          type: string
        endAt:
          type: ["string", "null"]
        allDay:
          type: boolean
        location:
          type: ["string", "null"]
        readOnly:
          type: boolean
        ownerId:
          type: integer
        eventId:
          type: integer
        taskId:
          type: integer
        channelId:
          type: integer
        messageId:
          type: integer
        sourceRef:
          type: object

    # TEAMS
    Team:
      type: object
      required: [id, name]
      properties:
        id:
          type: integer
        name:
          type: string
        color:
          type: ["string", "null"]
        memberCount:
          type: integer

    TeamInput:
      type: object
      required: [name]
      properties:
        name:
          type: string
        color:
          type: string

    JobTitleInput:
      type: object
      required: [name]
      properties:
        name:
          type: string

    NextEmployeeCode:
      type: object
      required: [employeeCode, year, sequence]
      properties:
        employeeCode:
          type: string
        year:
          type: integer
        sequence:
          type: integer

    # EMPLOYEES
    Employee:
      type: object
      required: [id, fullName, email, role]
      properties:
        id:
          type: integer
        fullName:
          type: string
        email:
          type: string
        role:
          type: string
        teamId:
          type: ["integer", "null"]
        teamName:
          type: ["string", "null"]
        jobTitle:
          type: ["string", "null"]
        avatarUrl:
          type: ["string", "null"]
        status:
          type: string
        baseSalary:
          type: ["number", "null"]
        bonus:
          type: ["number", "null"]
        payrollStatus:
          type: ["string", "null"]
        mustChangePassword:
          type: boolean
        phone:
          type: ["string", "null"]
        employeeCode:
          type: ["string", "null"]
        startDate:
          type: ["string", "null"]
        createdAt:
          type: string

    EmployeeInput:
      type: object
      required: [fullName, email, role]
      properties:
        fullName:
          type: string
        email:
          type: string
        password:
          type: string
        passwordMode:
          type: string
          enum: [auto, manual]
        role:
          type: string
        teamId:
          type: integer
        jobTitle:
          type: string
        phone:
          type: string
        employeeCode:
          type: string
        startDate:
          type: string
        baseSalary:
          type: number
        bonus:
          type: number
        status:
          type: string
        sendWelcomeEmail:
          type: boolean

    EmployeeCreateResponse:
      allOf:
        - $ref: "#/components/schemas/Employee"
        - type: object
          properties:
            emailSent:
              type: boolean
            message:
              type: string

    ResetPasswordInput:
      type: object
      properties:
        mode:
          type: string
          enum: [email, manual]
          description: email = generate temp password and email it; manual = set password in body
        password:
          type: string
          minLength: 8
          description: Required when mode is manual
        sendWelcomeEmail:
          type: boolean
          description: When mode is email, whether to email credentials (default true)

    MyProfileUpdate:
      type: object
      properties:
        avatarUrl:
          type: ["string", "null"]
        fullName:
          type: string
        phone:
          type: ["string", "null"]

    EmployeeUpdate:
      type: object
      properties:
        fullName:
          type: string
        email:
          type: string
        role:
          type: string
        teamId:
          type: integer
        jobTitle:
          type: string
        phone:
          type: string
        employeeCode:
          type: string
        startDate:
          type: string
        baseSalary:
          type: number
        bonus:
          type: number
        status:
          type: string
        payrollStatus:
          type: string

    # FINANCE
    FinanceSummary:
      type: object
      required: [totalPayroll, totalRevenue, totalExpenses, netProfit, pendingPayroll]
      properties:
        totalPayroll:
          type: number
        totalRevenue:
          type: number
        totalExpenses:
          type: number
        netProfit:
          type: number
        pendingPayroll:
          type: number

    SalaryRecord:
      type: object
      required: [userId, fullName, baseSalary]
      properties:
        userId:
          type: integer
        fullName:
          type: string
        teamName:
          type: ["string", "null"]
        jobTitle:
          type: ["string", "null"]
        baseSalary:
          type: number
        bonus:
          type: number
        totalPay:
          type: number
        payrollStatus:
          type: string

    Expense:
      type: object
      required: [id, description, amount, status]
      properties:
        id:
          type: integer
        description:
          type: string
        amount:
          type: number
        teamId:
          type: ["integer", "null"]
        teamName:
          type: ["string", "null"]
        submittedById:
          type: ["integer", "null"]
        submitterName:
          type: ["string", "null"]
        status:
          type: string
        createdAt:
          type: string

    ExpenseInput:
      type: object
      required: [description, amount]
      properties:
        description:
          type: string
        amount:
          type: number
        teamId:
          type: integer
        submittedById:
          type: integer

    SalaryPostingRecord:
      type: object
      required: [id, userId, fullName, allocationType, month, year, baseSalary, bonus, totalPay]
      properties:
        id:
          type: integer
        userId:
          type: integer
        fullName:
          type: string
        allocationType:
          type: string
          enum: [MONTHLY, PROJECT]
        projectId:
          type: ["integer", "null"]
        projectName:
          type: ["string", "null"]
        month:
          type: integer
        year:
          type: integer
        baseSalary:
          type: number
        bonus:
          type: number
        totalPay:
          type: number
        createdAt:
          type: string

    SalaryPostingInput:
      type: object
      required: [userId, allocationType, month, year, baseSalary, bonus]
      properties:
        userId:
          type: integer
        allocationType:
          type: string
          enum: [MONTHLY, PROJECT]
        projectId:
          type: integer
        month:
          type: integer
        year:
          type: integer
        baseSalary:
          type: number
        bonus:
          type: number

    PayrollRun:
      type: object
      required: [id, month, year, totalAmount, status]
      properties:
        id:
          type: integer
        month:
          type: integer
        year:
          type: integer
        totalAmount:
          type: number
        status:
          type: string
        approvedById:
          type: ["integer", "null"]
        createdAt:
          type: string

    PayrollRunInput:
      type: object
      required: [month, year]
      properties:
        month:
          type: integer
        year:
          type: integer

    # CLIENTS
    Client:
      type: object
      required: [id, contactName, companyName, email, status]
      properties:
        id:
          type: integer
        contactName:
          type: string
        companyName:
          type: string
        email:
          type: string
        phone:
          type: ["string", "null"]
        assignedTeamId:
          type: ["integer", "null"]
        assignedTeamName:
          type: ["string", "null"]
        status:
          type: string
        contractValue:
          type: ["number", "null"]
        nextMeetingAt:
          type: ["string", "null"]
        createdAt:
          type: string

    ClientInput:
      type: object
      required: [contactName, companyName, email]
      properties:
        contactName:
          type: string
        companyName:
          type: string
        email:
          type: string
        phone:
          type: string
        assignedTeamId:
          type: integer
        status:
          type: string
        contractValue:
          type: number
        nextMeetingAt:
          type: string

    ClientUpdate:
      type: object
      properties:
        contactName:
          type: string
        companyName:
          type: string
        email:
          type: string
        phone:
          type: string
        assignedTeamId:
          type: integer
        status:
          type: string
        contractValue:
          type: number
        nextMeetingAt:
          type: string

    # APPROVALS
    Approval:
      type: object
      required: [id, type, title, status]
      properties:
        id:
          type: integer
        type:
          type: string
        title:
          type: string
        description:
          type: ["string", "null"]
        amount:
          type: ["number", "null"]
        requestedById:
          type: integer
        requesterName:
          type: ["string", "null"]
        teamId:
          type: ["integer", "null"]
        teamName:
          type: ["string", "null"]
        status:
          type: string
        reviewedById:
          type: ["integer", "null"]
        reviewerName:
          type: ["string", "null"]
        reviewedAt:
          type: ["string", "null"]
        note:
          type: ["string", "null"]
        createdAt:
          type: string

    ApprovalInput:
      type: object
      required: [type, title]
      properties:
        type:
          type: string
        title:
          type: string
        description:
          type: string
        amount:
          type: number
        teamId:
          type: integer

    ApprovalReviewInput:
      type: object
      properties:
        note:
          type: string

    # REPORTS
    Report:
      type: object
      required: [id, type, title, period, generatedAt]
      properties:
        id:
          type: integer
        type:
          type: string
        title:
          type: string
        period:
          type: string
        generatedAt:
          type: string
        fileUrl:
          type: ["string", "null"]

    ReportInput:
      type: object
      required: [type, period]
      properties:
        type:
          type: string
        period:
          type: string
        title:
          type: string

    # CHANNELS & MESSAGES
    Channel:
      type: object
      required: [id, name, type]
      properties:
        id:
          type: integer
        name:
          type: string
        description:
          type: ["string", "null"]
        avatarUrl:
          type: ["string", "null"]
        teamId:
          type: ["integer", "null"]
        teamName:
          type: ["string", "null"]
        type:
          type: string
        visibility:
          type: string
        archived:
          type: boolean
        memberCount:
          type: integer
        myRole:
          type: ["string", "null"]
        unreadCount:
          type: integer
        lastPostAt:
          type: ["string", "null"]
        lastMessagePreview:
          type: ["string", "null"]
        lastReadMessageId:
          type: ["integer", "null"]
        createdById:
          type: ["integer", "null"]
        createdAt:
          type: string

    ChannelInput:
      type: object
      required: [name]
      properties:
        name:
          type: string
        description:
          type: string
        type:
          type: string
          enum: [TEAM, ANNOUNCEMENT]
        teamId:
          type: integer
        memberIds:
          type: array
          items:
            type: integer

    ChannelUpdate:
      type: object
      properties:
        name:
          type: string
        description:
          type: ["string", "null"]
        avatarUrl:
          type: ["string", "null"]
        archived:
          type: boolean

    ChannelMember:
      type: object
      required: [userId, fullName, role]
      properties:
        userId:
          type: integer
        fullName:
          type: string
        email:
          type: string
        avatarUrl:
          type: ["string", "null"]
        role:
          type: string
        joinedAt:
          type: string

    ChannelMemberInput:
      type: object
      required: [userId]
      properties:
        userId:
          type: integer
        role:
          type: string
          enum: [owner, member, viewer]

    MessageAttachment:
      type: object
      required: [type, url]
      properties:
        type:
          type: string
          enum: [image, file, video, audio]
        url:
          type: string
        name:
          type: string
        mimeType:
          type: string
        size:
          type: integer
        thumbUrl:
          type: string

    Message:
      type: object
      required: [id, channelId, senderId, body, createdAt]
      properties:
        id:
          type: integer
        channelId:
          type: integer
        senderId:
          type: integer
        senderName:
          type: string
        senderAvatar:
          type: ["string", "null"]
        body:
          type: string
        attachments:
          type: array
          items:
            $ref: "#/components/schemas/MessageAttachment"
        messageKind:
          type: string
          enum: [text, poll, event]
        metadata:
          type: object
        deleted:
          type: boolean
        createdAt:
          type: string

    MessageInput:
      type: object
      properties:
        body:
          type: string
        attachments:
          type: array
          items:
            $ref: "#/components/schemas/MessageAttachment"
        messageKind:
          type: string
          enum: [text, poll, event]
        metadata:
          type: object

    MentionCandidate:
      type: object
      required: [userId, fullName]
      properties:
        userId:
          type: integer
        fullName:
          type: string
        avatarUrl:
          type: ["string", "null"]

    MessageReactionInput:
      type: object
      required: [emoji]
      properties:
        emoji:
          type: string
          maxLength: 32

    PollVoteInput:
      type: object
      required: [optionId]
      properties:
        optionId:
          type: string

    EventRsvpInput:
      type: object
      required: [status]
      properties:
        status:
          type: string
          enum: [going, maybe, no]

    # ACTIVITY & NOTIFICATIONS
    ActivityLog:
      type: object
      required: [id, action, entityType, createdAt]
      properties:
        id:
          type: integer
        actorId:
          type: ["integer", "null"]
        actorName:
          type: ["string", "null"]
        action:
          type: string
        entityType:
          type: string
        entityId:
          type: ["integer", "null"]
        metadata:
          type: ["object", "null"]
        createdAt:
          type: string

    Notification:
      type: object
      required: [id, title, body, read, createdAt]
      properties:
        id:
          type: integer
        userId:
          type: integer
        title:
          type: string
        body:
          type: string
        read:
          type: boolean
        link:
          type: ["string", "null"]
        createdAt:
          type: string
