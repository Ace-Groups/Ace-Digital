# Local emulator / optional local functions dev only — never commit .env
# Generate: openssl rand -base64 48
JWT_SECRET=
# Resend.com API key (re_...) — get from https://resend.com/api-keys
# Also set in Render Dashboard env vars + firebase functions:secrets:set
RESEND_API_KEY=
# Must be a domain verified in your Resend dashboard
EMAIL_FROM=Ace-Digital <no-reply@acedigital.com>
# Google AI Studio key — also set via: firebase functions:secrets:set GEMINI_API_KEY
GEMINI_API_KEY=
