rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // All data access goes through Cloud Functions (Admin SDK).
    // Client apps must not read/write business data directly.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
